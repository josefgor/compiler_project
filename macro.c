#include<stdio.h>
#include<stdlib.h>
#include<string.h>

#include "opcode.h"
#include "extern.h"
#include "errorr.h"
#include "table.h"

void pre_assembler_spread_macros(char *line) 
{
	int i;
	char mcr[3]  = "mcr";

	for (i = 0; i = feof(*line); i++) {
		if(strcmp(line,table_macro_line(line))){
			
		}
	}
	return;

}

int macro_allready_in_table(char * macro) {
	
}
